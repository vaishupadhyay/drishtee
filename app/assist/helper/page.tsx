"use client";
import { useState } from "react";
import Link from "next/link";
import { AssistShell } from "@/components/assist/assist-shell";
export default function HelperPage() { const [option, setOption] = useState("self"); return <AssistShell eyebrow="Trusted Helper Mode" title="Choose the support that feels right."><div className="mt-8 grid gap-3">{[["self", "I&apos;m filing this myself", "Continue independently."], ["assist", "Someone is helping me file this", "A family member, caregiver or support worker is beside you."], ["behalf", "I&apos;m filing on someone else&apos;s behalf", "Record that you are helping another person." ]].map(([id, title, text]) => <button key={id} onClick={() => setOption(id)} className={`min-h-20 rounded-2xl border p-4 text-left ${option === id ? "border-assist-blue bg-sky" : "border-assist-line bg-white"}`}><strong className="block text-sm">{title}</strong><span className="mt-1 block text-xs text-[#617087]">{text}</span></button>)}</div>{option !== "self" && <div className="mt-5 rounded-2xl border border-assist-line bg-white p-4"><label className="block text-sm font-bold">Helper name <input className="mt-2 min-h-12 w-full rounded-xl border border-assist-line px-3" placeholder="Optional" /></label><label className="mt-3 block text-sm font-bold">Relationship <input className="mt-2 min-h-12 w-full rounded-xl border border-assist-line px-3" placeholder="Family member, caregiver…" /></label></div>}<Link href="/assist/draft" className="mt-6 inline-flex min-h-12 items-center rounded-full bg-navy px-5 text-sm font-extrabold text-white">Continue to guided drafting</Link></AssistShell>; }

import Link from "next/link";
import { AssistShell } from "@/components/assist/assist-shell";

export default function AssistScanPage() { return <AssistShell eyebrow="Scan" title="Point your camera at the place or notice."><p className="mt-5 max-w-[55ch] text-[17px] leading-7 text-[#526074]">A sign, counter, notice, receipt or token can help confirm the location. This uses the existing Scan and Report capture journey.</p><div className="mt-8 rounded-[28px] border border-assist-line bg-sky p-6 text-center"><div className="mx-auto grid aspect-[4/3] max-w-md place-items-center rounded-2xl border-2 border-sky-strong bg-white"><span className="grid h-20 w-20 place-items-center rounded-2xl border border-sky-strong text-4xl text-sky-strong">⌁</span></div><p className="mt-4 text-sm font-bold text-[#526074]">Fit the sign or document inside the frame.</p></div><Link href="/scan" className="mt-6 inline-flex min-h-12 items-center rounded-full bg-navy px-5 text-sm font-extrabold text-white">Open camera capture →</Link></AssistShell>; }

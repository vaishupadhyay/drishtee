"use client";
import { useState } from "react";
import Link from "next/link";
import { AssistShell } from "@/components/assist/assist-shell";

export default function SpeakPage() { const [listening, setListening] = useState(false); const [transcript, setTranscript] = useState(""); return <AssistShell eyebrow="Speak" title="Just talk. We&apos;ll write it down."><div className="mt-9 text-center"><button onClick={() => { setListening((value) => !value); if (!listening && !transcript) setTranscript("I need help with a public service issue near me."); }} className={`mx-auto grid h-36 w-36 place-items-center rounded-full border-8 text-5xl transition ${listening ? "border-mint-strong bg-mint text-mint-strong" : "border-sky-strong bg-sky text-sky-strong"}`} aria-pressed={listening} aria-label={listening ? "Stop listening" : "Start listening"}>🎙️</button><p className="mt-5 text-sm font-bold text-[#526074]">{listening ? "Listening… tap again when you&apos;re done." : "Tap once to start or stop."}</p></div><div className="mt-8 rounded-2xl border border-assist-line bg-white p-5"><p className="text-[11px] font-extrabold uppercase tracking-[.11em] text-assist-blue">Live transcript</p><p className="mt-3 min-h-20 text-xl leading-8 text-navy">{transcript || "Your words will appear here in large text."}</p>{!listening && transcript && <p className="mt-4 rounded-xl bg-mint px-3 py-2 text-sm font-semibold text-mint-strong">Got it. Reading this back to you… <span className="font-normal">Simulated read-aloud in this prototype.</span></p>}</div><Link href="/assist/type" className="mt-5 inline-flex min-h-12 items-center text-sm font-bold text-assist-blue underline underline-offset-4">or type instead</Link></AssistShell>; }

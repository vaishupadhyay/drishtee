"use client";
import { useState } from "react";
import { AssistShell } from "@/components/assist/assist-shell";
import { ReadAloudButton } from "@/components/assist/read-aloud-button";
import { SolvedConfirm } from "@/components/assist/solved-confirm";
export default function StatusPage() { const [explanation, setExplanation] = useState(""); const status = "Your report has been received and someone is looking into it."; return <AssistShell eyebrow="Report status" title="Here&apos;s what&apos;s happening."><div className="mt-7 rounded-2xl border border-assist-line bg-mint p-5"><div className="flex flex-wrap items-center justify-between gap-3"><p className="text-lg font-extrabold text-navy">{status}</p><ReadAloudButton text={status} /></div></div><div className="mt-5 rounded-2xl border border-assist-line bg-white p-5"><p className="text-xs font-extrabold uppercase tracking-wide text-assist-blue">Latest response</p><p className="mt-3 text-sm leading-6 text-[#526074]">The matter has been reviewed and the relevant team has been asked to take the next step.</p><button onClick={() => setExplanation("The team has seen the report and has asked the people responsible for this service to check what happened. You should receive another update when they respond.")} className="mt-4 min-h-12 rounded-full border border-assist-line px-4 text-sm font-extrabold text-navy">Explain this response</button>{explanation && <p className="mt-4 rounded-xl bg-sky p-3 text-sm leading-6 text-[#526074]"><strong>AI explanation: </strong>{explanation}</p>}</div><SolvedConfirm /></AssistShell>; }

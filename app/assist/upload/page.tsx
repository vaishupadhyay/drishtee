"use client";
import { useState } from "react";
import Link from "next/link";
import { AssistShell } from "@/components/assist/assist-shell";

export default function UploadPage() { const [files, setFiles] = useState<File[]>([]); return <AssistShell eyebrow="Upload" title="Bring the document you already have."><label className="mt-8 flex min-h-56 cursor-pointer flex-col items-center justify-center rounded-2xl border-2 border-dashed border-assist-blue bg-sky p-6 text-center"><span className="text-4xl">📎</span><strong className="mt-3 text-lg">Tap to choose a photo or document</strong><span className="mt-2 text-sm text-[#617087]">Images and PDF files are accepted.</span><input className="sr-only" type="file" accept="image/*,application/pdf" multiple onChange={(event) => setFiles(Array.from(event.target.files ?? []))} /></label>{files.length > 0 && <div className="mt-5 space-y-3">{files.map((file, index) => <div key={`${file.name}-${index}`} className="flex min-h-14 items-center justify-between rounded-xl border border-assist-line bg-white px-4"><span className="truncate text-sm font-semibold">{file.name}</span><button onClick={() => setFiles((current) => current.filter((_, fileIndex) => fileIndex !== index))} className="min-h-12 px-2 text-sm font-bold text-alert">Remove</button></div>)}</div>}<p className="mt-6 text-[17px] font-semibold">Want to tell us more about this, or is the document enough?</p><div className="mt-3 flex flex-wrap gap-3"><Link href="/assist/type" className="flex min-h-12 items-center rounded-full border border-assist-line bg-white px-5 text-sm font-extrabold">Add a few words</Link><Link href="/assist" className="flex min-h-12 items-center rounded-full bg-navy px-5 text-sm font-extrabold text-white">This is enough</Link></div></AssistShell>; }

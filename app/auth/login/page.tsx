"use client";
import { useState } from "react";
import Link from "next/link";
import { AssistShell } from "@/components/assist/assist-shell";
export default function LoginPage() { const [mode, setMode] = useState("mobile"); const [otp, setOtp] = useState(""); return <AssistShell eyebrow="Sign in" title="Your report is ready when you are."><div className="mt-7 flex gap-2 rounded-xl bg-surface-2 p-1"><button onClick={() => setMode("mobile")} className={`min-h-12 flex-1 rounded-lg text-sm font-extrabold ${mode === "mobile" ? "bg-white text-navy" : "text-[#617087]"}`}>Mobile + OTP</button><button onClick={() => setMode("email")} className={`min-h-12 flex-1 rounded-lg text-sm font-extrabold ${mode === "email" ? "bg-white text-navy" : "text-[#617087]"}`}>Email</button></div><div className="mt-5 rounded-2xl border border-assist-line bg-white p-5"><label className="text-sm font-bold">{mode === "mobile" ? "Mobile number" : "Email address"}<input className="mt-2 min-h-12 w-full rounded-xl border border-assist-line px-3" inputMode={mode === "mobile" ? "tel" : "email"} /></label><label className="mt-4 block text-sm font-bold">Verification code <input value={otp} onChange={(event) => setOtp(event.target.value)} className="mt-2 min-h-12 w-full rounded-xl border border-assist-line px-3" placeholder="Demo code: 123456" /></label><Link href="/assist/review" className="mt-5 flex min-h-12 items-center justify-center rounded-full bg-navy text-sm font-extrabold text-white">Continue securely</Link></div><Link href="/auth/signup" className="mt-5 inline-flex text-sm font-bold text-assist-blue underline underline-offset-4">Create an account instead</Link></AssistShell>; }

"use client";
import { useState } from "react";
import Link from "next/link";
import { ReadAloudButton } from "@/components/assist/read-aloud-button";
const questions: [string, string[]][] = [["When did this happen?", ["Today", "This week", "Longer ago"]], ["What outcome are you hoping for?", ["Service restored", "Clear explanation", "Someone to contact me"]]];
export function QuestionStepper() { const [step, setStep] = useState(0); const [answer, setAnswer] = useState(""); const [question, choices] = questions[step]; const next = step === questions.length - 1; return <div className="mt-8"><p className="text-xs font-extrabold uppercase tracking-[.12em] text-assist-blue">Question {step + 1} of {questions.length}</p><div className="mt-3 rounded-2xl border border-assist-line bg-white p-5"><div className="flex items-start justify-between gap-3"><h2 className="text-2xl font-extrabold leading-8">{question}</h2><ReadAloudButton text={question} /></div><div className="mt-6 grid gap-3">{choices.map((choice) => <button key={choice} onClick={() => setAnswer(choice)} className={`min-h-14 rounded-xl border px-4 text-left text-sm font-bold ${answer === choice ? "border-assist-blue bg-sky text-assist-blue" : "border-assist-line bg-white"}`}>{choice}</button>)}</div></div>{next ? <Link href="/assist/review" className="mt-6 inline-flex min-h-12 items-center rounded-full bg-navy px-5 text-sm font-extrabold text-white">Review my report</Link> : <button disabled={!answer} onClick={() => { setStep(1); setAnswer(""); }} className="mt-6 min-h-12 rounded-full bg-navy px-5 text-sm font-extrabold text-white disabled:opacity-40">Continue</button>}</div>; }

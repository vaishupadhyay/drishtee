"use client";
import { useState } from "react";
export function ReadAloudButton({ text }: { text: string }) { const [playing,setPlaying]=useState(false);const [failed,setFailed]=useState(false);function speak(){if(!("speechSynthesis" in window)){setFailed(true);return;}window.speechSynthesis.cancel();const utterance=new SpeechSynthesisUtterance(text);utterance.onend=()=>setPlaying(false);utterance.onerror=()=>{setPlaying(false);setFailed(true);};setPlaying(true);window.speechSynthesis.speak(utterance);}return <span className="inline-flex flex-col items-end"><button onClick={speak} className="assist-button inline-flex items-center border border-assist-line bg-white px-3 text-sm">{playing?"Reading…":"Read aloud"}</button>{failed&&<span className="assist-micro mt-1 text-alert">Audio is unavailable; you can keep reading.</span>}</span>; }

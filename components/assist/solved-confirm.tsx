"use client";
import { useState } from "react";
export function SolvedConfirm() { const [choice, setChoice] = useState<"yes" | "no" | null>(null); return <div className="mt-7 rounded-2xl border border-assist-line bg-white p-5"><h2 className="text-xl font-extrabold">Did this solve the issue?</h2><div className="mt-4 grid gap-3 sm:grid-cols-2"><button onClick={() => setChoice("yes")} className="min-h-16 rounded-xl bg-mint px-4 text-sm font-extrabold text-mint-strong">Yes, this solved it</button><button onClick={() => setChoice("no")} className="min-h-16 rounded-xl bg-alert-tint px-4 text-sm font-extrabold text-alert">No, not solved</button></div>{choice && <p className="mt-4 text-sm text-[#526074]">{choice === "yes" ? "Thank you. We&apos;ve recorded that this is resolved." : "We&apos;ll help you prepare the next response in the next step."}</p>}</div>; }

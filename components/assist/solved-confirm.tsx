"use client";
import { useState } from "react";
export function SolvedConfirm(){const [choice,setChoice]=useState<"yes"|"no"|null>(null);return <div className="assist-card mt-7"><h2 className="assist-title">Did this solve the issue?</h2><div className="mt-4 grid gap-3 sm:grid-cols-2"><button onClick={()=>setChoice("yes")} className="assist-button min-h-14 bg-mint px-4 text-mint-strong shadow-[var(--elev-2)]">Yes, this solved it</button><button onClick={()=>setChoice("no")} className="assist-button min-h-14 bg-alert-tint px-4 text-alert shadow-[var(--elev-2)]">No, not solved</button></div>{choice&&<p className="assist-body mt-4 text-[#526074]">{choice==="yes"?"Thank you. We’ve recorded that this is resolved.":"We’ll help you prepare the next response."} <button onClick={()=>setChoice(null)} className="assist-link">Undo</button></p>}</div>;}

export type Attribute = { label: string; value: string };
export function UnderstandingCard({ attributes }: { attributes: Attribute[] }) { return <div className="mt-7 space-y-3">{attributes.map(({ label, value }) => <div key={label} className="flex min-h-14 items-center justify-between gap-4 rounded-2xl border border-assist-line bg-white px-4"><span><strong className="block text-xs uppercase tracking-wide text-assist-blue">{label}</strong><span className="block text-sm font-bold text-navy">{value}</span></span><button className="min-h-12 px-2 text-sm font-bold text-assist-blue" aria-label={`Edit ${label}`}>Edit</button></div>)}</div>; }
